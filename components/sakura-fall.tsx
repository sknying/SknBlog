// Compatibility export. The actual implementation belongs to the Spring
// package so pages can keep their existing import while the theme stays portable.
export { SakuraFall } from "@/themes/spring/components/sakura-fall";
